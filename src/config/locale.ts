/** ISO 4217 code for business amounts (Italy). */
export const APP_CURRENCY = "EUR" as const;

/** BCP 47 locale for currency, numbers, and common date formatting. */
export const APP_LOCALE = "it-IT" as const;
